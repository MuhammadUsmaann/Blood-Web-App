'use strict';
app.controller('searchresultsController', ['$scope', '$rootScope', function ($scope, $rootScope) {
    $rootScope.layoutClass = "contact-form fixed-nav";
    $rootScope.title = "Search Result";
    $rootScope.fixHeight = 0;
}]);